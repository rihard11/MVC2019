@model List<Zadaca_4._1._vj.Models.Artikl>
@{
    ViewBag.Title = "PrikaziKosaricu";
    Layout = "~/Views/Shared/_OsnovniLayout.cshtml";
}

<h2>PrikaziKosaricu</h2>
@foreach (var item in Model)
{
    // @item.Naziv<br />
    @Html.Partial("_Artikl", item)
}
